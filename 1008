#include<stdio.h>
int main()
{
    int a,b;
    float c,sum;
    scanf("%d %d %f",&a,&b,&c);
      sum=(b*c);
    printf("NUMBER = %d\n",a);
    //sum=(b*c);
    printf("SALARY = U$ %.2f\n",sum);
    return 0;
}
